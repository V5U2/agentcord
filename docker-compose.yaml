services:
  agentcord:
    build:
      context: .
    image: ghcr.io/v5u2/agentcord:dev
    restart: unless-stopped
    extra_hosts:
      - host.docker.internal:host-gateway
    volumes:
      - ./config.yaml:/app/config.yaml:ro
